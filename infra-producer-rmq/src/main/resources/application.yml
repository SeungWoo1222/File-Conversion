server:
  port: 8082
spring:
  application:
    name: cdc-event
  profiles:
    active: local
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASS}